import React from "react";
import ReactDOM from "react-dom/client";
import SocialLoginDemo from "../SocialLoginDemo";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <SocialLoginDemo />
  </React.StrictMode>
);
